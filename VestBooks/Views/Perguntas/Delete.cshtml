@model Pergunta
@{
    ViewData["Title"] = "Excluir Pergunta";
    ViewData["Menu"] = "Perguntas";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>@ViewData["Title"]</h1>
    <a asp-action="Index" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left"></i> Voltar para Lista
    </a>
</div>

<div class="row">
    <div class="col-md-8 mx-auto">
        <div class="card border-danger">
            <div class="card-header bg-danger text-white">
                <h5 class="card-title mb-0">
                    <i class="fas fa-exclamation-triangle"></i> Confirmação de Exclusão
                </h5>
            </div>
            <div class="card-body">
                <div class="alert alert-warning" role="alert">
                    <h5 class="alert-heading">
                        <i class="fas fa-exclamation-circle"></i> Atenção!
                    </h5>
                    <p class="mb-0">
                        Você está prestes a excluir a pergunta e todas as suas @Model.PerguntaAlternativas.Count alternativas. 
                        Esta ação <strong>não pode ser desfeita</strong>.
                    </p>
                </div>

                <div class="mb-3">
                    <h6>Detalhes da Pergunta:</h6>
                    <table class="table table-sm table-borderless">
                        <tr>
                            <td class="fw-bold" style="width: 120px;">ID:</td>
                            <td>@Model.PerguntaId</td>
                        </tr>
                        <tr>
                            <td class="fw-bold">Faculdade:</td>
                            <td>@Model.Faculdade?.Nome</td>
                        </tr>
                        <tr>
                            <td class="fw-bold">Alternativas:</td>
                            <td>@Model.PerguntaAlternativas.Count</td>
                        </tr>
                    </table>
                </div>

                <div class="mb-3">
                    <h6>Enunciado:</h6>
                    <div class="card p-3 bg-light">
                        <div style="white-space: pre-line;">@Html.Raw(Model.Enunciado?.Replace("<br>", "\n"))</div>
                    </div>
                </div>

                <form asp-action="Delete" method="post">
                    <input type="hidden" asp-for="PerguntaId" />
                    
                    <div class="mt-4">
                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <button type="submit" class="btn btn-danger me-md-2">
                                <i class="fas fa-trash"></i> Confirmar Exclusão
                            </button>
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="fas fa-times"></i> Cancelar
                            </a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>